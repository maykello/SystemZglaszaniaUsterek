@model SystemZglaszaniaUsterek.Controllers.PriorityFormModel
@{
    ViewData["Title"] = "Nowy priorytet";
}

<div class="form-page-container">
    <div class="container">

        <div class="form-page-header">
            <h1 class="form-page-title">Nowy priorytet</h1>
            <p class="form-page-subtitle">Dodaj nowy poziom priorytetu do systemu</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-4">
                <div class="form-card">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="form-group mb-4">
                            <label asp-for="Name" class="form-label"></label>
                            <input asp-for="Name" class="form-control" placeholder="Np. Niski, Średni, Wysoki..." />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-actions">
                            <button type="submit" class="btn-submit-ticket">Utwórz priorytet</button>
                            <a asp-action="Index" class="btn-cancel-ticket">Anuluj</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>

@section Scripts { @{await Html.RenderPartialAsync("_ValidationScriptsPartial");} }
