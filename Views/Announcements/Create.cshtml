@model SystemZglaszaniaUsterek.Models.ViewModels.AnnouncementFormViewModel
@using SystemZglaszaniaUsterek.Models.Enums
@{
    ViewData["Title"] = "Nowe ogłoszenie";
}

<div class="form-page-container">
    <div class="container">

        <div class="form-page-header">
            <h1 class="form-page-title">Nowe ogłoszenie</h1>
            <p class="form-page-subtitle">Opublikuj ogłoszenie widoczne dla użytkowników systemu</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-9 col-lg-7">
                <div class="form-card">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="form-group mb-3">
                            <label asp-for="Title" class="form-label"></label>
                            <input asp-for="Title" class="form-control" placeholder="Krótki, zwięzły tytuł ogłoszenia" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Content" class="form-label"></label>
                            <textarea asp-for="Content" class="form-control" rows="6" placeholder="Treść ogłoszenia..."></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Severity" class="form-label"></label>
                            <select asp-for="Severity" asp-items="Html.GetEnumSelectList<AnnouncementSeverity>()" class="form-select"></select>
                        </div>

                        <div class="form-check mb-4 ps-0" style="padding-left: 0;">
                            <div class="d-flex align-items-center gap-2">
                                <input asp-for="IsActive" class="form-check-input mt-0" style="width: 20px; height: 20px; flex-shrink: 0;" />
                                <label asp-for="IsActive" class="form-label mb-0">Aktywne (widoczne od razu)</label>
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="submit" class="btn-submit-ticket">Opublikuj ogłoszenie</button>
                            <a asp-action="Index" class="btn-cancel-ticket">Anuluj</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>

@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
